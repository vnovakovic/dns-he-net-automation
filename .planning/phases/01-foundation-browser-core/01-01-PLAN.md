---
phase: 01-foundation-browser-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - go.mod
  - go.sum
  - cmd/server/main.go
  - internal/config/config.go
  - internal/config/config_test.go
  - internal/model/types.go
  - internal/store/sqlite.go
  - internal/store/sqlite_test.go
  - internal/store/migrations/001_init.sql
  - Dockerfile
autonomous: true
requirements:
  - OPS-03
  - OPS-06
  - REL-01
  - SEC-03

must_haves:
  truths:
    - "Go module initializes and all dependencies resolve"
    - "Config loads from environment variables with correct defaults"
    - "SQLite database opens in WAL mode with foreign_keys=ON and busy_timeout=5000"
    - "Goose migrations run automatically on startup creating the schema"
    - "Database file is created with 0600 permissions"
  artifacts:
    - path: "go.mod"
      provides: "Go module definition with all Phase 1 dependencies"
      contains: "github.com/playwright-community/playwright-go"
    - path: "internal/config/config.go"
      provides: "Config struct with env tags and Load function"
      exports: ["Config", "Load"]
    - path: "internal/store/sqlite.go"
      provides: "SQLite initialization with WAL mode and goose migrations"
      exports: ["Open"]
    - path: "internal/store/migrations/001_init.sql"
      provides: "Initial database schema"
      contains: "+goose Up"
    - path: "internal/model/types.go"
      provides: "Shared domain types"
      exports: ["Account", "Zone", "Record"]
    - path: "cmd/server/main.go"
      provides: "Entry point with signal handling"
      min_lines: 30
  key_links:
    - from: "cmd/server/main.go"
      to: "internal/config"
      via: "config.Load()"
      pattern: "config\\.Load"
    - from: "cmd/server/main.go"
      to: "internal/store"
      via: "store.Open(cfg.DBPath)"
      pattern: "store\\.Open"
    - from: "internal/store/sqlite.go"
      to: "internal/store/migrations/"
      via: "embed.FS with goose Provider"
      pattern: "go:embed migrations"
---

<objective>
Project scaffolding: Go module, config, domain types, SQLite store with goose migrations, and a minimal main.go entry point with signal handling.

Purpose: Establish the project foundation that all subsequent plans build upon -- module dependencies, configuration system, database layer, and service entry point.
Output: A compilable Go project with working config loading, SQLite database initialization (WAL mode, goose migrations), and graceful signal handling skeleton.
</objective>

<execution_context>
@C:/Users/vladimir/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/vladimir/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-browser-core/01-RESEARCH.md
@.planning/phases/01-foundation-browser-core/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Go module, dependencies, project skeleton, config, and domain types</name>
  <files>
    go.mod
    go.sum
    internal/config/config.go
    internal/config/config_test.go
    internal/model/types.go
  </files>
  <action>
Initialize Go module and install all Phase 1 dependencies:

```bash
go mod init github.com/vnovakovic/dns-he-net-automation
go get github.com/playwright-community/playwright-go@v0.5700.1
go get modernc.org/sqlite@latest
go get github.com/pressly/goose/v3@latest
go get github.com/caarlos0/env/v11@latest
go get github.com/google/uuid@latest
go get -t github.com/stretchr/testify@latest
```

Create `internal/config/config.go`:
- Define `Config` struct with `env` struct tags using caarlos0/env v11:
  - `Port int` (env:"PORT", default:"8080")
  - `DBPath string` (env:"DB_PATH", default:"dns-he-net.db")
  - `HEAccountsJSON string` (env:"HE_ACCOUNTS", required)
  - `PlaywrightHeadless bool` (env:"PLAYWRIGHT_HEADLESS", default:"true")
  - `PlaywrightSlowMo float64` (env:"PLAYWRIGHT_SLOW_MO", default:"0")
  - `OperationTimeoutSec int` (env:"OPERATION_TIMEOUT_SEC", default:"30")
  - `OperationQueueTimeoutSec int` (env:"OPERATION_QUEUE_TIMEOUT_SEC", default:"60")
  - `LogLevel string` (env:"LOG_LEVEL", default:"info")
  - `MinOperationDelaySec float64` (env:"MIN_OPERATION_DELAY_SEC", default:"1.5")
  - `SessionMaxAgeSec int` (env:"SESSION_MAX_AGE_SEC", default:"1800") -- 30 min proactive re-login
- Define `func Load() (*Config, error)` using `env.ParseAs[Config]()` (generics API from env v11).
- IMPORTANT: Use `github.com/caarlos0/env/v11` (NOT v6, v10). The v11 API uses `env.ParseAs[T]()` which returns `(T, error)`, not `*T`.

Create `internal/config/config_test.go`:
- Test that Load() with minimal env vars (just HE_ACCOUNTS) returns defaults for Port=8080, DBPath="dns-he-net.db", PlaywrightHeadless=true, etc.
- Test that Load() without HE_ACCOUNTS returns an error (required field).
- Use `t.Setenv()` for env var isolation.

Create `internal/model/types.go`:
- Package `model` with shared domain types:
  - `Account` struct: ID string, Username string, CreatedAt time.Time, UpdatedAt time.Time
  - `Zone` struct: ID string, Name string, AccountID string
  - `Record` struct: ID string, ZoneID string, Type string, Name string, Content string, TTL int, Priority int (for MX/SRV), Weight int (for SRV), Port int (for SRV), Target string (for SRV), Dynamic bool (DDNS checkbox)
  - `RecordType` string type with constants for all 17 HE types: A, AAAA, CNAME, ALIAS, MX, NS, TXT, CAA, AFSDB, HINFO, RP, LOC, NAPTR, PTR, SSHFP, SPF, SRV

SEC-03 note: Credentials are NEVER stored in model types or database. The `Account` struct stores only metadata (id, username). Passwords live only in the credential provider (env var in Phase 1, Vault in Phase 4).
  </action>
  <verify>
Run `go build ./...` -- must compile with zero errors.
Run `go test ./internal/config/...` -- both tests must pass.
Run `go vet ./...` -- no warnings.
  </verify>
  <done>
go.mod exists with all Phase 1 dependencies pinned. Config loads from env vars with correct defaults. Domain types defined. All compile and vet clean.
  </done>
</task>

<task type="auto">
  <name>Task 2: SQLite store with goose migrations and main.go entry point</name>
  <files>
    internal/store/sqlite.go
    internal/store/sqlite_test.go
    internal/store/migrations/001_init.sql
    cmd/server/main.go
    Dockerfile
  </files>
  <action>
Create `internal/store/migrations/001_init.sql` (goose format):
```sql
-- +goose Up
CREATE TABLE IF NOT EXISTS accounts (
    id         TEXT PRIMARY KEY,
    username   TEXT NOT NULL UNIQUE,
    created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE IF NOT EXISTS schema_info (
    key   TEXT PRIMARY KEY,
    value TEXT NOT NULL
);

INSERT INTO schema_info (key, value) VALUES ('version', '1');

-- +goose Down
DROP TABLE IF EXISTS schema_info;
DROP TABLE IF EXISTS accounts;
```

NOTE: Password is NOT in this table (SEC-03). Credentials come from CredentialProvider.

Create `internal/store/sqlite.go`:
- Package `store`
- `//go:embed migrations/*.sql` for embedded migrations
- `func Open(dbPath string) (*sql.DB, error)`:
  1. If dbPath is not ":memory:", check if file exists. If not, create with `os.OpenFile(path, O_CREATE|O_RDWR, 0600)` then close (SEC-03: 0600 permissions).
  2. Build DSN: `file:{dbPath}?_pragma=journal_mode(WAL)&_pragma=busy_timeout(5000)&_pragma=foreign_keys(1)` (REL-01). CRITICAL: Use driver name `"sqlite"` (modernc.org/sqlite), NOT `"sqlite3"`.
  3. `sql.Open("sqlite", dsn)` then `db.PingContext(ctx)`.
  4. Create goose provider: `fs.Sub(embedMigrations, "migrations")` then `goose.NewProvider(goose.DialectSQLite3, db, migrationFS)`.
  5. Run `provider.Up(context.Background())`.
  6. Log each applied migration with slog.Info.
  7. Return `*sql.DB`.
- Import `_ "modernc.org/sqlite"` for driver registration (blank import).

Create `internal/store/sqlite_test.go`:
- Test Open with ":memory:" -- must succeed, run migrations, accounts table exists.
- Test Open with temp file -- verify file permissions are 0600 (on Linux/Mac; skip permission check on Windows).
- Test that PRAGMA journal_mode returns "wal" via `db.QueryRow("PRAGMA journal_mode").Scan(&mode)`.
- Test that PRAGMA foreign_keys returns 1.

Create `cmd/server/main.go`:
- Package `main`
- Configure slog JSON handler at startup based on config.LogLevel (OPS-03): parse level string to slog.Level, set as default handler with `slog.SetDefault(slog.New(slog.NewJSONHandler(os.Stdout, &slog.HandlerOptions{Level: level})))`.
- Load config via `config.Load()`.
- Open SQLite via `store.Open(cfg.DBPath)`.
- Set up signal handling with `signal.NotifyContext(context.Background(), syscall.SIGTERM, syscall.SIGINT)`.
- Log startup: "service starting" with port, db_path, headless, slow_mo fields. NEVER log HE_ACCOUNTS value (SEC-03).
- On signal: log "shutting down", close DB, exit 0.
- For now, just block on context.Done() after setup (browser and HTTP server added in plans 01-02 and Phase 2).

Create `Dockerfile` (multi-stage):
- Stage 1 "modules": `golang:1.24`, copy go.mod/go.sum, `go mod download`.
- Stage 2 "builder": `golang:1.24`, copy cached modules, copy source, `CGO_ENABLED=0 GOOS=linux GOARCH=amd64 go build -o /bin/server ./cmd/server`. Extract playwright-go version from go.mod: `grep playwright-go go.mod | awk '{print $2}'`, then `go install github.com/playwright-community/playwright-go/cmd/playwright@{version}`.
- Stage 3 "runtime": `ubuntu:noble`, install ca-certificates + tzdata, copy playwright CLI from builder, `playwright install --with-deps chromium`, copy server binary, set PLAYWRIGHT_HEADLESS=true, EXPOSE 8080, ENTRYPOINT ["server"].
  </action>
  <verify>
Run `go test ./internal/store/...` -- all tests pass (WAL mode verified, migrations applied, permissions checked).
Run `go build -o /dev/null ./cmd/server` -- compiles successfully.
Run a quick smoke test: `HE_ACCOUNTS='[{"id":"test","username":"test","password":"test"}]' go run ./cmd/server &` then send SIGINT -- verify "shutting down" log appears and process exits cleanly. Kill the background process if it does not exit.
  </verify>
  <done>
SQLite store opens with WAL mode, busy_timeout, foreign_keys pragmas. Goose migration 001 runs creating accounts and schema_info tables. Database file created with 0600 permissions. main.go starts, loads config, opens DB, handles SIGTERM/SIGINT cleanly. Dockerfile exists with multi-stage build.
  </done>
</task>

</tasks>

<verification>
- `go build ./...` succeeds with zero errors
- `go test ./...` passes all tests (config defaults, config required field, SQLite pragmas, migration)
- `go vet ./...` reports no issues
- main.go starts with valid env vars and exits cleanly on SIGTERM
- No credentials appear in any log output
</verification>

<success_criteria>
- Go module exists with playwright-go v0.5700.1, modernc.org/sqlite, goose v3, env v11, uuid, testify
- Config struct loads all env vars with correct types and defaults
- SQLite opens in WAL mode with foreign_keys=ON and busy_timeout=5000
- Database file permissions are 0600
- Goose migration 001_init.sql creates accounts and schema_info tables
- main.go compiles, starts, logs JSON, handles signals, exits cleanly
- Domain types defined for Account, Zone, Record with all 17 RecordType constants
- Dockerfile builds (structure correct, not tested in this plan)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-browser-core/01-01-SUMMARY.md`
</output>
